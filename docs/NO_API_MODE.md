NO-API mode:
- Upload CSVs with product candidates and supplier links
- The scraper and sourcer agents will operate on the CSV rows instead of using external APIs

Place CSVs in data/ and use backend endpoints to ingest.
